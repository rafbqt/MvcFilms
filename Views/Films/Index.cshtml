@model List<MvcFilms.Models.Film>

<h1>Liste des films</h1>

<ul>
@foreach (var film in Model)
{
    <li>
        @film.Titre — @film.Realisateur — @film.Annee
        <a asp-action="Details" asp-route-id="@film.Id">Détails</a>
    </li>
}
</ul>